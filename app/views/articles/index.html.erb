<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">
    <% @articles.each do |a| %>
    <div class="col-xs-12 col-sm-3">
      <%= render "card", article: a %>
    </div>
    <% end %>
  </div>
</div>

<%= link_to 'New Article', new_article_path %>
